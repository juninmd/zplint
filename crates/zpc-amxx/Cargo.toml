[package]
name = "zpc-amxx"
version = "0.1.0"
edition = "2024"
description = ".amxx container writer (format reimplemented from spec)"

[dependencies]
zpc-diag = { path = "../zpc-diag" }
flate2 = "1"
